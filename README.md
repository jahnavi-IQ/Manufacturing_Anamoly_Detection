📝 Usage - Now You Can Run:

Start API:
# Instead of long uvicorn command
cd api
conda activate sound_cls(environment)
python main.py

Start UI:
# Instead of long streamlit command
cd ui
conda activate sound_cls(environment)
streamlit run app.py